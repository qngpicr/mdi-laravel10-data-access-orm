<?php

return [

    /*
    |--------------------------------------------------------------------------
    | CORS Paths
    |--------------------------------------------------------------------------
    | 어떤 경로에 대해 CORS를 적용할지 지정합니다.
    | 일반적으로 API 엔드포인트와 Sanctum 관련 경로를 포함합니다.
    */
    // 'paths' => ['api/*', 'sanctum/csrf-cookie'],
    'paths' => ['api/*', 'routes', 'sanctum/csrf-cookie'],


    /*
    |--------------------------------------------------------------------------
    | Allowed Methods
    |--------------------------------------------------------------------------
    | 허용할 HTTP 메서드. '*'로 모든 메서드를 허용하거나 배열로 제한할 수 있습니다.
    */
    'allowed_methods' => ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],

    /*
    |--------------------------------------------------------------------------
    | Allowed Origins
    |--------------------------------------------------------------------------
    | 허용할 Origin(프론트엔드 주소). '*'는 모든 Origin을 허용합니다.
    | 실무에서는 보안상 특정 도메인만 지정하는 것이 권장됩니다.
    */
    // 'allowed_origins' => ['http://localhost:3000', 'https://www.example.com'],
    'allowed_origins' => ['http://localhost:9041'],


    /*
    |--------------------------------------------------------------------------
    | Allowed Origins Patterns
    |--------------------------------------------------------------------------
    | 정규식 패턴으로 Origin을 허용할 수 있습니다.
    */
    'allowed_origins_patterns' => [],

    /*
    |--------------------------------------------------------------------------
    | Allowed Headers
    |--------------------------------------------------------------------------
    | 허용할 요청 헤더. '*'로 모든 헤더를 허용하거나 배열로 제한할 수 있습니다.
    */
    'allowed_headers' => ['Content-Type', 'Authorization', 'X-Requested-With'],

    /*
    |--------------------------------------------------------------------------
    | Exposed Headers
    |--------------------------------------------------------------------------
    | 브라우저에서 접근 가능한 응답 헤더.
    */
    'exposed_headers' => [],

    /*
    |--------------------------------------------------------------------------
    | Max Age
    |--------------------------------------------------------------------------
    | Preflight(OPTIONS) 요청 결과를 캐시할 시간(초).
    */
    'max_age' => 7200,

    /*
    |--------------------------------------------------------------------------
    | Supports Credentials
    |--------------------------------------------------------------------------
    | 쿠키/세션 등 Credential을 허용할지 여부.
    */
    'supports_credentials' => true,

];
